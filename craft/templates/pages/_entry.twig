{% extends '_layout' %}
{% block main %}

<div class="container">
    <h1>{{ entry.title }}</h1>
    <div class="row">
        <div class="col">
            {{ entry.pageCopy }}
        </div>
    </div>
</div>

{% endblock %}